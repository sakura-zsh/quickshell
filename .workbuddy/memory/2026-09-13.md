# 2026-09-13

## 任务栏从左改到顶部（完整横向化重构，sakura 选定方案）

**成果**：`modules/bar/` 由竖向架构改为顶部横向，提交 `e87bce6`（重构前快照 `a977298` 可回滚）。

**根因**：原 bar 是硬编码竖向架构，不是配置项。竖向假设分布在 20 个文件：
- `BarWrapper.qml`：`implicitWidth` + `anchors.right` + states 动画宽度
- `Bar.qml`：根 `ColumnLayout`、spacer `Layout.fillHeight`、`childAt(width/2, y)`/`checkPopout(y)`/`handleWheel(y)` 全按 y 轴
- `ActiveWindow.qml`：`Rotation { angle: 90/270 }` —— 就是"标题躺着放"的根因
- `Clock.qml`：`implicitWidth: innerWidth` + 时间格式 `"hh\nmm"` 竖排
- `drawers/`：`Drawers.qml` mask/regions、`Exclusions/Border/Backgrounds/Panels/Interactions` 共 8 处 `bar.implicitWidth` 定位

**改造方法**：
- 保留 `Config.bar.sizes.innerWidth` 字段名，横向后作为 bar 的**厚度（高度）**使用，避免用户配置迁移
- 轴向翻转对照：`implicitWidth↔implicitHeight`、`ColumnLayout↔RowLayout`、`fillHeight↔fillWidth`、`topMargin↔leftMargin`、`childAt(width/2,y)↔childAt(x,height/2)`、`anchors.left/right↔anchors.top/bottom`
- drawers 定位偏移：水平偏移由 `bar.implicitWidth` 改为 `Config.border.thickness`，垂直偏移由 `Config.border.thickness` 改为 `bar.implicitHeight`
- `Panels.qml` 的 popouts 定位：`y` 跟随 `currentCenter` 改为 `x` 跟随，`y` 固定贴顶

## ⚠️ 排查陷阱（重要）

1. **bar 改完后完全消失，一度误判为布局 bug** —— 实际是 **quickshell 进程已崩溃**（热重载大改动 + 反复 reload 后实例挂掉）。判断方法：`pgrep -x quickshell` 无输出即为进程不存在，而非代码问题。
2. **热重载对这种量级的改动不可靠**：多次 reload 后会出现"Configuration Loaded 但组件不实例化"的状态。改完应**重启实例**：`pkill -x quickshell; setsid quickshell -c /home/sakura/.config/quickshell > /tmp/qs.log 2>&1 &`
3. **全屏窗口会遮挡 layer-shell top 层**：niri 中窗口全屏时 bar 不可见（截图验证时容易误判为 bar 没渲染），验证前需退出全屏。
4. 调试手段：`quickshell log -t N` 读日志；`quickshell ipc call drawers toggle dashboard` / `quicktoggles open` 可在无鼠标模拟的情况下验证面板定位（`xdotool` 在 Wayland 下无效，`ydotool`/`wtype` 未安装）。

## 遗留项

- `workspaces/context/` 5 个文件（右键上下文菜单）仍是竖向：ContextBg、ContextIndicator、ItemWorkspaceContext、MultiWindowContext、WindowIconContext
- `WindowIcon.qml` / `DraggableWindowColumn.qml` 已做轴翻转但**未实测**（用户配置 `showWindows: false`，功能未启用）
- bar popouts（状态图标点击弹出面板）定位代码已改，未实测鼠标悬停触发

## popout 与 bar 连接修复（20:50 追加）

- 用户反馈：托盘/右侧按钮 popout 与 bar 连接不自然，且关闭后白背景残留。
- 残留根因：Wrapper.qml 的 nonAnimWidth 条件 `x > 0 || hasCurrent` 是左侧 bar 时代逻辑（贴 bar 时 x==0）；横向化后 x 恒为居中位置 >0，关闭时宽度永不收敛，Shape 融合背景不消失。
- 修复：Background.qml 顶部直角融合；Wrapper 改为高度收合（implicitHeight: open ? H : 0 + Behavior），宽度直接设定；Content Loader 锚点镜像为 bottom+horizontalCenter。
- 验证方法：Panels.qml 临时加 Timer 强制 popouts.hasCurrent=true + 自动 close，grim 截图对比开/关两态，再移除 Timer 冷重启。

## context 卡片化修复（21:50 追加）

- 用户反馈右键工作区图标有 bug：原忠实轴翻转产生窄竖条（宽度=图标宽），文字全被裁切，且 workspace 类型下所有窗口图标的卡片在同一 y 下重叠。
- 修复：扩展条改为 250px 宽下拉卡片（ItemWorkspaceContext/MultiWindowContext/ContextBg 三者对齐、左对齐于图标）；窗口卡片仅在 item 类型渲染（WindowIcon.contextLoader active 条件 + implicitHeight 增长都加 wsContextType==="item"）。
- workspace 类型 context 会联动打开 WorkspacesPopout（wsWindow，拖拽窗口用），是预期功能不是 bug。
- 顺带修上游 bug：Niri.cleanWindowTitle 不存在（定义在 dashboard/ActiveWindow.qml），已补进 Niri.qml 并给调用处补 app_id 参数。
- launcher 被 dock 遮挡：drawers 主窗口在 launcher 打开时提升到 WlrLayer.Overlay。
- 调试技巧：给 Workspace 的 windows Loader 和 DraggableWindowColumn 的 Row 加了 objectName，可从外部定位 WindowIcon 强制 context 状态截图验证。

## dock/启动器启动 Electron 应用失败（21:55 追加）

- 根因：今天多次重启 quickshell 时用的是 WorkBuddy 内嵌终端（setsid quickshell &），把 ELECTRON_RUN_AS_NODE=1 等几十个 WorkBuddy 环境变量带进 quickshell；dock/launcher 通过 execDetached 继承 quickshell 环境启动应用 → 所有 Electron 应用（Cider 等）被以纯 Node 模式拉起，无窗口即"启动失败"。niri spawn-at-startup 与用户终端环境干净所以正常。
- 修复：pkill 后用 `niri msg action spawn -- quickshell -c ~/.config/quickshell` 重启（继承 niri 干净环境）。
- 规则：**以后凡是从 WorkBuddy shell 重启 quickshell，必须走 niri msg action spawn，或至少 env -u ELECTRON_RUN_AS_NODE**，不要直接 setsid。
- 已实测 cider 经 niri spawn 正常启动。

## Clavis 组件移植第一批（22:15 追加）

- 全局 StateLayer 水波纹升级为 M3 并行扩散+淡出（Canvas 版，源自 Clavis RippleEffect），一处改动全 shell 生效。
- AppearanceConfig 补 expressive 曲线/时长（slowSpatial/fastEffects/slowEffects）。
- 调试技巧：StateLayer 新增公共 rippleAt(x,y)，可用 Timer 自动触发+grim 连拍验证波纹帧。
- 候选后续：MaterialWaveProgressBar(音量/进度条波浪)、RadialSpectrum+AudioSpectrum(环形频谱)、StyledSwitch 形变、CookieClock/SystemBatteryTank(dashboard)、WeatherBackground。

## Clavis 移植第二批（22:35 追加）

- WaveSlider 波浪音量条落地在音频 popout；开关旋钮换弹性曲线；Visualiser 贴左。
- 天气：服务 16 天+逐小时（Open-Meteo），WeatherPanel 成为 dashboard 第 4 个 tab，概览天气卡可点击直达（state.currentTab=3）。
- 坑：Open-Meteo daily 变量名是 wind_direction_10m_dominant（不是 _degree 后缀）；Material 图标名 wb_sunny（weather_sunny 不存在会显示文字）。
- 频谱环按用户条件跳过；Cava.values 可随时复用。
- 注意：天气 IP 定位走了代理显示 Singapore，用户可设 Config.services.weatherLocation 固定城市。

## 动态天气背景（23:10 追加）

- WeatherBackground+LeafItem 拷入 modules/dashboard/，当前天气卡换成天空场景（weatherCode 驱动雨/雪/雷/流星/云/叶粒子，夜间自动夜空）。
- 坑：Clavis 同目录组件（LeafItem）必须一起拷，漏了会启动崩溃；MeteoIcon 依赖 qs.Common+Qt.labs.lottieqt，未移植。
- 验证：独立实例（模块解析要求测试文件放 config 目录内，/tmp 下 pragma 失效）+grim 截图确认夜空渲染。

## 滚轮修复（23:20 追加）
- 嵌套 Flickable 里 WheelHandler 会被外层 dashboard Flickable 抢走滚轮；改用 MouseArea(acceptedButtons: NoButton).onWheel 手动调 contentX，accept 阻断传播。可靠。

## 风级显示（23:30 追加）
- Weather.beaufortText(km/h)→"N 级 中文名"；风卡与 16 天卡已切换。城市已被用户固定为 Luoyang。

## 滚轮二次修复（23:40 追加）
- 根因确认：Flickable interactive:false 连原生滚轮一起禁用 + Flickable 会把内部 MouseArea 挂到 contentItem。
- 正确模式（与 Calendar 一致）：条带卡片顶层放 CustomMouseArea 覆盖层，120 累积阈值分步滚动 contentX，事件不进 Flickable。日历月切换已用此模式多年，可靠。
- 电池油箱按用户要求取消。

## 滚轮用户实测通过（23:35 追加）
- 用户确认天气条带滚轮正常可用（Calendar 同款 CustomMouseArea 模式）。Clavis 移植到此收工，CookieClock/ExpressiveMetricTile 等候选未采用。

## 收工点（用户叫停）
- 当前全部已提交并推送（origin/main 同步）。无未完成改动。
- 待办（用户已同意、下次继续）：#17 CookieClock 弹跳时钟替换 DateTime 卡；#18 ExpressiveMetricTile 替换 Resources 竖条。
- Clavis 源：~/Documents/quickshell；电池油箱已取消；频谱环已跳过。
