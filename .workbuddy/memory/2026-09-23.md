
## bar 布局重排：窗口标题移到左侧、时钟移到中间（09-23）

- 需求：activeWindow 从中间挪到 workspaces 右侧；clock 从右侧挪到中间。
- 实现：只改 shell.json 的 bar.entries 顺序 —— logo, workspaces, activeWindow, spacer, clock, spacer, tray, divider, statusIcons, divider, power, idleInhibitor。
- 配套代码改动：新增 Config.bar.activeWindow.maxWidth（默认 0 = 原行为），用户配置设为 360。原因：ActiveWindow.maxWidth 原本 = bar.width - 其他模块宽度 - 间距 - 内边距，且计算时把 spacer 排除在"其他模块"之外，等于可吃光 spacer 的空间；标题长时会挤掉 spacer，把居中的 clock 顶偏。
- 分隔线变化：原 [tray] | [clock statusIcons] | [power] 变为 [tray] | [statusIcons] | [power]（clock 移出该组到中央）。
- 未验证：本轮命令环境无法访问用户会话（niri/quickshell 不可见、grim "failed to create display"，沙箱内外皆然），截图与热重载验证未完成，待用户目视确认。

## bar 三项改进：统一背景色 / 时钟秒数 / 居中补偿（09-23 第二轮）

- 需求：① 窗口标题与托盘加背景色且与时钟一致 ② 时钟偏右需修正 ③ 时钟显示秒数。
- 背景色统一令牌：Colours.tPalette.m3surfaceContainer（clock/tray/status/workspaces 全用这个）。ActiveWindow 本次新增 background 选项并改用 StyledRect 根，内边距用 padding.md(=10)，与 clock/tray 开启背景后的内边距一致。
- 秒数：Clock 新增 showSeconds，格式 hh:mm:ss（12 小时制 hh:mm:ss A）；Time.format 走 Qt.formatDateTime，SystemClock 精度已是 Seconds，等宽字体保证宽度不跳。
- 居中根因：两个 fill spacer 把中间模块放在"空闲区中心"，不是屏幕中心；偏移 = (左组宽 - 右组宽)/2。修法：给较窄一侧的 spacer 加 Layout.preferredWidth = 两侧组宽差，使中间模块落到真中心。仅当恰好 2 个 enabled spacer 时生效，否则退回原行为。
- 未验证：本轮命令环境仍无法访问用户会话（niri/quickshell 不可见、grim failed to create display），截图与运行验证未完成。
- 遗留待定：logo（OsIcon）未加背景（用户只点名标题与托盘），如需要可再加。

## bar 居中补偿修复 + 药丸高度对齐（09-23 第三轮）

- 用户反馈：时钟改后"太靠左"，且某部件背景上下高度不够。
- 用截图做像素测量（图像 2560x70，屏幕宽 2560，中心 1280）：
  * 药丸 x 区间：workspaces 224..359 / activeWindow 378..550 / clock 565..745(中心 654, 偏左 626px) / tray 2200..2350 / status 2368..2486。
  * 垂直范围：workspaces、clock、tray、status 均 y 5..44 (高 40)；activeWindow 仅 y 13..36 (高 24) → 高度不够的就是它（用户口述为 "status 部件"）。
- 复位根因：上一版居中补偿把 spacer 自身宽度也算进两侧求和 → 自反馈 → 补偿值放大到 ≈空闲空间总量(F≈1338)，把时钟推到紧贴左组。
- 修法：只用 spacer 索引做区间运算（求和区间 [0, idx0) 与 [idx1+1, count) 由索引推出，spacer 结构上无法进入求和），另加 width*0.25 安全钳制。
- 高度修法：ActiveWindow implicitHeight 从 row.implicitHeight(24) 改为 Config.bar.sizes.innerWidth(40)。
- 实测脚本留在 ~/.cache/qs-crops/（measure*.py），下次给新截图可直接复测标定。
- 仍未运行验证（命令环境无法访问会话）。

## 时钟位置手动旋钮（09-23 第四轮）

- 用户要求自己调时钟位置。新增 Config.bar.clock.offset（int, px, 正数右移/负数左移），默认 0，已写入用户 shell.json 的 bar.clock.offset。
- 实现：Bar.qml spacerPreferredWidth 中 manual = off>0 ? 加到左 spacer(2*off) : 加到右 spacer(-2*off)；auto 与 manual 各自独立钳制在条宽 25% 以内，保证手动偏移始终有满量程（±320px @2560）。
- 换算关系：offset 值 = 时钟需要移动的像素数（1:1 生效，因为两端各挪一半）。
- 用户自行调整方式：改 shell.json 数字即可，Config 热重载。

## 推送 + GitHub Action 失败排查（09-23 第五轮）

### 推送
- 沙箱内 git push 必然失败（无网络）；提权后命令以 root 身份执行，且 ssh 默认通道报 "Bad owner or permissions on /etc/ssh/ssh_config.d/20-systemd-ssh-proxy.conf"（该软链接目标属主为 nobody，mtime 09-23 04:40，系统侧变化）→ ssh 异常退出 → git 报 "无法读取远程仓库"。
- 可用绕法（已成功推送 8accbc7..08dbe95）：
  git -c core.sshCommand="ssh -F /dev/null -i /home/sakura/.ssh/id_ed25519 -o IdentitiesOnly=yes -o UserKnownHostsFile=/home/sakura/.ssh/known_hosts -o StrictHostKeyChecking=accept-new" push
- 注意：提权后 ssh 用 getpwuid 的 home（/root）找 known_hosts，故必须显式指定 UserKnownHostsFile。

### Action 失败
- 仓库只有一个会跑的 workflow：.github/workflows/update-flake-inputs.yml（每周日 00:00 UTC 定时 + 手动）。release.yml 只在 v* tag 触发，从未运行。
- 两次运行（#1 09-13 head 40e9e17、#2 09-20 head 8accbc7）均失败；失败步骤固定为第 7 步 "Test on Sway"（exit 1），之前 flake update / nix build 全部成功。
- 该步骤逻辑：装 sway → headless 起 sway → result/bin/caelestia-shell -d → sleep 3 → pgrep .quickshell-wra（注释写明 "Fail job if shell died"）→ 退出码 1 说明 shell 在该环境没活下来。
- 日志需鉴权（本环境 gh token 失效），未能取到具体行；公开 API 只能拿到步骤级结论 + 注解（仅 "Process completed with exit code 1" + node20/ubuntu 迁移提示）。
- 待用户决定：删掉 CI（个人配置仓库意义不大）或保留 flake 更新但去掉/放宽 sway 冒烟测试。

### 属主问题（重要发现）
- 本环境提权命令以 root 运行，且 chown 被拒（EINVAL，数字 uid 也不行，无 CAP_CHOWN）。
- 现状：仓库 1568 个文件属主 root；~/.config 限深 3 共 3879 个条目属主 root；niri_caelestia/shell.json 也是 root。
- 影响：sakura 以自己身份无法写这些 644 文件（改配置需要 sudo）。
- 需用户在真实终端执行：sudo chown -R sakura:sakura ~/.config/quickshell ~/.config/niri_caelestia
  以及系统 ssh 配置修复：sudo chown -R root:root /etc/ssh/ssh_config.d /usr/lib/systemd/ssh_config.d

## CI 删除 + 属主修复受阻（09-23 第六轮）

- 用户选方案 A：已删除 .github/workflows/{update-flake-inputs.yml,release.yml}，提交 101d606 并推送；GitHub API 确认 actions/workflows total_count = 0（周日不再跑、不再报红）。.github/ISSUE_TEMPLATE 与 CONTRIBUTING.md 保留（上游遗留，用户未要求删）。
- 属主修复：用户称两条 chown 已执行，但我这边复核仍为旧状态（/etc/ssh/ssh_config.d 及 /usr/lib/systemd/ssh_config.d 均 nobody:nobody；~/.config/quickshell 仍 root:root；仓库 root 属主文件 1575）。用户消息里两条命令是**连写无分隔**（".../ssh_config.dsudo chown -R sakura:sakura ..."），推测第一条把后续内容当文件参数，第二条根本没独立执行。
- 本环境 chown 返回 EINVAL（无 CAP_CHOWN），无法代劳；且此环境可能看不到用户终端里的最新改动（待验证）。
- 正确命令（一行一条）：
  sudo chown -R root:root /etc/ssh/ssh_config.d
  sudo chown -R root:root /usr/lib/systemd/ssh_config.d
  sudo chown -h root:root /etc/ssh/ssh_config.d/20-systemd-ssh-proxy.conf   # 软链自身需 -h
  sudo chown -R sakura:sakura ~/.config/quickshell
  sudo chown -R sakura:sakura ~/.config/niri_caelestia ~/.config/niri
- 验证：ssh -G github.com 无输出即 ssh 配置已修好；stat -c '%U:%G %n' 检查属主。

## CI 删除 + 属主修复受阻（09-23 第六轮）

- 用户选方案 A：已删除 .github/workflows/{update-flake-inputs.yml,release.yml}，提交 101d606 并推送；GitHub API 确认 actions/workflows total_count = 0（周日不再跑、不再报红）。.github/ISSUE_TEMPLATE 与 CONTRIBUTING.md 保留（上游遗留，用户未要求删）。
- 属主修复：用户称两条 chown 已执行，但我这边复核仍为旧状态（/etc/ssh/ssh_config.d 及 /usr/lib/systemd/ssh_config.d 均 nobody:nobody；~/.config/quickshell 仍 root:root；仓库 root 属主文件 1575）。用户消息里两条命令是**连写无分隔**（".../ssh_config.dsudo chown -R sakura:sakura ..."），推测第一条把后续内容当文件参数，第二条根本没独立执行。
- 本环境 chown 返回 EINVAL（无 CAP_CHOWN），无法代劳；且此环境可能看不到用户终端里的最新改动（待验证）。
- 正确命令（一行一条）：
  sudo chown -R root:root /etc/ssh/ssh_config.d
  sudo chown -R root:root /usr/lib/systemd/ssh_config.d
  sudo chown -h root:root /etc/ssh/ssh_config.d/20-systemd-ssh-proxy.conf   # 软链自身需 -h
  sudo chown -R sakura:sakura ~/.config/quickshell
  sudo chown -R sakura:sakura ~/.config/niri_caelestia ~/.config/niri
- 验证：ssh -G github.com 无输出即 ssh 配置已修好；stat -c '%U:%G %n' 检查属主。
